export default function tuple() {
  // 一般的なタプルの型定義
  let response: [number, string] = [200, "OK"]
  // response = [400, "Bad request", "Email"]
  // response = ["400", "Bad request"]
  console.log("taple sample 1:", response)
}